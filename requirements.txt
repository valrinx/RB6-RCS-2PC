# Core (required)
fastapi>=0.129.0
uvicorn[standard]>=0.40.0
pydantic>=2.12.15
pynput>=1.7.6          # Rapid Fire — physical mouse state tracking

# Hardware controllers (install only what you use)
makcu>=2.3.1           # MAKCU 2-PC hardware
kmbox-universal>=0.1.1 # KMBox Net/Pro 2-PC hardware
